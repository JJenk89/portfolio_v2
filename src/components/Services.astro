---
import "../styles/global.css";
---

<section class="services">
	<h2 id="services">Services</h2>
	<div class="cards">
		<div class="card">
			<h3>Web Design</h3>
			<div class="icon">ICON</div>
			<p>
				I design clean, user-focused websites using modern tools like
				Figma. My work prioritises intuitive UX, elegant UI, and visual
				clarity while ensuring SEO best practices are built in from the
				start.
			</p>
		</div>
	</div>
</section>
<style>
	.services {
		color: var(--colour-primary);
		font-family: var(--font-primary);
		height: 100vh;
	}

	h2 {
		font-weight: var(--extra-light);
		font-size: 2rem;
		font-style: italic;
		text-align: center;
	}

	.card {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-items: center;
	}

	h3 {
		font-family: var(--font-secondary);
		font-weight: var(--regular);
	}

	p {
		font-weight: var(--light);
	}
</style>
